/**
 * Enum representing the possible modes for the patient modal
 */
export enum ModalMode {
  /** Mode for editing an existing patient */
  Edit = 'edit',
  /** Mode for creating a new patient */
  Create = 'create',
}

